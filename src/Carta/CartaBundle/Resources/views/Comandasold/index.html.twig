{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Comandas list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Platoid</th>
                <th>Cantidad</th>
                <th>Fecha</th>
                <th>Clienteid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('comandas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.platoId }}</td>
                <td>{{ entity.cantidad }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.clienteId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('comandas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('comandas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('comandas_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
