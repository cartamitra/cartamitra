{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PrecioTotal</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Tipoprecio</th>
                <td>{{ entity.tipoPrecio }}</td>
            </tr>
            <tr>
                <th>Platoid</th>
                <td>{{ entity.platoId }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ entity.precio }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('preciototal') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('preciototal_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('preciototal_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
