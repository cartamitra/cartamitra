{% extends '::base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/stylemio.css') }}" type="text/css" rel="stylesheet" />
{% endblock %}
{% block body -%}
    <h1>Plato</h1>
        <li>    
    </a>
            <a href="{{ path('plato_new') }}">
            Crear uno nuevo
    </a>
        </li>
    <table class="record_properties" border="1">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Fotoid</th>
                <td>{{ entity.fotoId }}</td>
            </tr>
            <tr>
                <th>Tipoid</th>
                <td>{{ entity.tipoId }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ entity.precio }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('plato') }}">
            Back to the list
    </li>
    <li>
        <a href="{{ path('plato_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('plato_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
