{% extends '::base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/stylemio.css') }}" type="text/css" rel="stylesheet" />
{% endblock %}
{% block body -%}
    <h1>Plato list</h1>
   <li>    
    </a>
            <a href="{{ path('plato_new') }}">
            Crear uno nuevo
    </a>
        </li>
    <table class="records_list" border="1">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Fotoid</th>
                <th>Tipoid</th>
                <th>Precio</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('plato_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.fotoId }}</td>
                <td>{{ tiponombre[loop.index0] }}</td>
                <td>{{ entity.precio }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('plato_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('plato_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('plato_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
